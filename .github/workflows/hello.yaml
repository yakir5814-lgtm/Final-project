name: hello-actions

on:
  push:
    branches: [ "main", "dev" ]
  workflow_dispatch: # שורה זו תציג את כפתור ההרצה הידנית

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello GitHub Actions"

  build:
    runs-on: ubuntu-latest
    needs: hello
    steps:
      - name: do build
        run: echo "building app"
