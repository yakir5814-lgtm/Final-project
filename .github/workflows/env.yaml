name: env-actions

on:
  push:
    branches: [ "main", "dev" ]

env:
  COURSE_NAME: "DevOps"

jobs:
  log-env:
    runs-on: ubuntu-latest
    steps:
      - name: Print environment variable
        run: echo "Welcome to the ${{ env.COURSE_NAME }} final project!"
